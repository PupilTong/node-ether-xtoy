{
  "name": "ether-xtoy",
  "version": "0.0.1",
  "description": "a ether_aton, ether_ntoa, ether_ntohost and ether_hostton bindings for Node.js",
  "main": "index.js",
  "scripts": {
    "install": "prebuild-install --runtime napi --tag-prefix '' || node-gyp rebuild",
    "build": "node-gyp rebuild",
    "build:dev": "node-gyp rebuild --debug",
    "test": "mocha src/tests"
  },
  "keywords": [
    "ethernet",
    "ether_ntoa",
    "ether_aton"
  ],
  "author": "pupilTong",
  "license": "Apache-2.0",
  "dependencies": {
    "mocha": "^9.1.3",
    "node-addon-api": "^4.2.0",
    "node-gyp": "^8.4.1",
    "should": "^13.2.3"
  }
}
